import {Animated} from 'react-native';
import styled from 'styled-components/native';

export const Container = styled(Animated.View)<any>`
  position: absolute;
  width: 100%;
  bottom: 25px;
  align-self: center;
`;
